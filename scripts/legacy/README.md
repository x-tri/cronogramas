# scripts/legacy — Não usar

Arquivos de deploy de iterações anteriores do XTRI Cronogramas:
VPS Docker (212.85.19.50), Coolify, Vercel, nginx self-hosted.

**O fluxo de deploy atual é Hostinger via GitHub Actions** — ver
`../../README.md` e `.github/workflows/deploy-hostinger.yml`.

Mantidos por histórico/auditoria. Não referenciados em CI, package.json
ou no `deploy-hostinger.sh` ativo (verificado em 2026-05-22).
